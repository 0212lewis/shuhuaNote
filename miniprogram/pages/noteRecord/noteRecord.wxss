/* pages/noteRecord/noteRecord.wxss */

.wrapper {
    width: 100%;
    padding-top: 54px;
    padding-bottom: calc(160rpx + env(safe-area-inset-bottom));
}

.record-wrapper > .hasMore {
    text-align: center;
    color: #969799;
    font-size: 24rpx;
}